<% if current_user.present? && current_user.likes?(product) %>
  <%= link_to "Unlike", product_like_path(product.id), method: :delete, remote: true %>
<% else %>
  <%= link_to "Like", product_like_path(product.id), method: :post, remote: true %>
<% end %>

<%= product.likes.count %>

